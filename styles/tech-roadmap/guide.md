# 技术路线图（tech-roadmap）

本目录提供「技术路线图」的 draw.io 样式参考。

## 目录内容

- `style.md` —— 技术路线图样式规范与完整示例（时间轴 / 分层、模块、里程碑、XML 样例）。

## 怎么用

1. 用户未指定样式时，直接读 `style.md`，按其规范与示例手写 draw.io XML。
2. 严格遵守通用层 `drawio.md` 与 `prompt.md` 的格式与输出要求（只输出 XML）。

# 泳道图（跨职能流程图，swimlane）

本目录提供「泳道图 / 跨职能流程图」的 draw.io 样式参考。

## 目录内容

- `style.md` —— 泳道图样式规范与完整示例（泳道、阶段、节点、连接、XML 样例）。

## 怎么用

1. 用户未指定样式时，直接读 `style.md`，按其规范与示例手写 draw.io XML。
2. 严格遵守通用层 `drawio.md` 与 `prompt.md` 的格式与输出要求（只输出 XML）。
